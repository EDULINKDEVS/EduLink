import { Box } from '@mui/material';
import React from 'react';

const Custom404 = () => {
  return (
    <Box sx={{
        height: '100vh',
        width: '100%',
        fontSize: '3em',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'custom'
    }}>
      Spierdalaj
    </Box>
  );
};

export default Custom404;
